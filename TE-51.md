# TE-51 — Landing comercial Eunomi Escolar

Entrada HTML en `eunomi-escolar/index.html`, compilada por Vite en `/eunomi-escolar/`. El sitio raíz mantiene su entrada independiente. La política existente conserva su contenido y recibe estilos coherentes y un enlace de retorno a la landing. No depende de la API ni de Supabase.

## Contenido y mantenimiento

- Paleta y logo extraídos de `mobile/lib/core/theme/app_colors.dart` y `mobile/assets/branding/login_mark.png` del proyecto móvil.
- Contacto confirmado: `iv.bozo.catalan@gmail.com` y `https://wa.me/56941068150`.
- Estado comercial: en desarrollo; captación de interesados, sin contratación, registro, pagos ni promesas de pilotos disponibles.
- Teléfono: representación conceptual con datos ficticios, no captura real. Reemplazar por capturas aprobadas al estar disponibles.
- Planes: editar la sección `modalidades` cuando TE-37 esté validada.
- Privacidad: enlace a la política existente. La separación por organización se describe como trabajo en curso.

## Validación

- `npm run build` y `git diff --check`: correctos.
- Navegador de escritorio (1440 × 1000) y móvil (390 × 844): carga, imágenes, ausencia de desbordamiento y errores de página.
- FAQ desplegable y anclas internas verificadas.
- Destinos WhatsApp y correo comprobados sin enviar mensajes.
- La instalación reporta 15 vulnerabilidades en dependencias preexistentes (1 baja, 4 moderadas y 10 altas); esta tarea no cambia dependencias. La landing no carga el bundle React.

## Mejoras tras auditoría

- Mensaje inicial explícito, problemas cotidianos y papel del transportista, conductor y apoderado.
- `src/eunomi/content.ts`: contenido tipado del ejemplo, perfiles y constantes de contacto.
- `src/eunomi/main.ts`: menú móvil accesible, ejemplo de cuatro pasos, selector de perfil, WhatsApp personalizado y copia de correo con alternativa ante fallo.
- HTML con contenido esencial y pasos visibles sin JavaScript. Las mejoras se habilitan al inicializarse.
- `public/eunomi-escolar/styles.css`: diseño y adaptaciones móviles; `privacy-theme.css`: presentación de la política.
- `logo-web.png`: exportación de 128 px y 13491 bytes, conservando el original.
- Bundle propio: aproximadamente 2,02 KB gzip más polyfill de Vite; sin dependencias nuevas.
- Validación: compilación, ESLint de archivos TS modificados, diff; pantallas de 320, 390, 768 y 1440 px; perfiles, límites del ejemplo, reinicio, menú/Escape y copia de correo. Contenido sin JavaScript verificado en una pestaña aislada con ejecución desactivada mediante CDP.
- No se afirma conformidad WCAG ni se han realizado pruebas de comprensión con clientes reales.

## Entrega

Rama `feat/TE-51-eunomi-landing`. Vista previa en Vercel para revisión visual. Publicación en el dominio principal pendiente; no se ha hecho merge ni modificado la base de datos.
